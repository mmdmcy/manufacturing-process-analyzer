# Database Configuration
db.url=jdbc:mysql://localhost:3306/manufacturing_db
db.username=root
db.password=password

# Application Settings
app.name=Manufacturing Process Analyzer
app.version=1.0.0

# Logging Configuration
log.level=INFO
log.filepath=logs/app.log

# Analysis Settings
analysis.enable=true
analysis.threshold=0.05

# Reporting Settings
reporting.format=PDF
reporting.outputPath=reports/